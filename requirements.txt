fastapi==0.111.0
uvicorn[standard]==0.29.0
python-pptx==0.6.23
Pillow==10.3.0
pydantic==2.7.1
# playwright is pre-installed in mcr.microsoft.com/playwright/python base image
# do NOT add it here — causes version conflicts
